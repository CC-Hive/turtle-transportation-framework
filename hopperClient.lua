local transportRequestAPI = dofile("transportRequestAPI.lua")

if not transpoted yet then
  request boarding
  get list of destinations
  set destination
  say ready
  wait
else
  confirm transport
  get luggage
  say thanks
end